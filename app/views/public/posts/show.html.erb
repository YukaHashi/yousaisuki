<div class="container">
<h3>投稿詳細</h3>
  <div class="container">
    <%= image_tag @post.get_image , size: "200x200" %>
    <p>タイトル：<%= @post.title %></p>
    <p>説明：<%= @post.body %></p>
    <p>コメント数：<%= @post.post_comments.count %></p>
    <p>投稿日：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
    <p>投稿ユーザー：<%= image_tag @post.user.get_profile_image(100,100), class: "mt-3 rounded-circle content-center mr-3"; %><%= @post.user.name %></p>
    
    <% if @post.user == current_user %>
      <%= link_to "編集", edit_post_path(@post.id), class: "btn btn-primary" %>
      <%= link_to "削除", post_path(@post), class: "btn btn-danger", method: :delete, "data-confirm" => "本当に削除しますか？" %>
    <% end %>
    
    <div class="container w-75 mt-5 my-3 ml-3 mr-3">
      <div>
        <p><strong>コメント一覧</strong></p>
        <p><%= render "public/post_comments/index", post: @post %></p>
      </div>
      <div>
        <p>新規コメント投稿</p>
        <%= render "public/post_comments/form", post: @post, post_comment: @post_comment %>
      </div>
    </div>
</div>
